import { createTool } from "@mastra/core/tools";
import { z } from "zod";
import { zapierMcp } from "../../../integrations/zapier-mcp.js";

// REMOVED: All common tools (validateSession, getSystemInfo, getHelp, zapierAiQuery, searchFAQDatabase) - not currently used

export const commonTools = {};
